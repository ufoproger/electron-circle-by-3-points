/* global THREE */

Object.assign(THREE.Line3.prototype, {
  /*
  enlarge: function (length, optionalTarget) {
    let result = optionalTarget || new THREE.Line3()
    let v = new THREE.Vector3()

    this.delta(v).normalize().multiplyScalar(length)
    result.copy(this)

    // result.start.sub(v)
    // result.end.add(v)

    return result
  },

  middlePerpendicular (normal, optionalTarget) {
  }
*/
})
